spring:
  datasource:
    url: jdbc:postgresql://prod-db-server:5432/orderdb_prod
    username: prod_order_user
    password: prod_password
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
